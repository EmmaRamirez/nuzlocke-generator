# Database configuration (defaults work for local development)
# DATABASE_URL=ecto://postgres:postgres@localhost/nuzlocke_api_dev

# GitHub API token for releases and bug reporting
GH_ACCESS_TOKEN=your_github_personal_access_token

# Guardian secret key for JWT (generate with: mix guardian.gen.secret)
# Required in production
# GUARDIAN_SECRET_KEY=your_secret_key_here

# Phoenix secret key base (generate with: mix phx.gen.secret)
# Required in production
# SECRET_KEY_BASE=your_secret_key_here

