import { css } from 'emotion';

export const iconBar = css`
    align-items: center;
    display: flex;
    margin-left: auto;
    * {
        margin: 0.25rem;
    }
`;

export const copyButton = css`
    color: lightblue;
    margin-left: auto;
    cursor: pointer;
`;
