import { css } from 'emotion';

export const iconBar = css`
    align-items: center;
    display: flex;
    margin-left: auto;
    * {
        margin: .25rem;
    }
`;

export const copyButton = css`
    color: lightblue;
    margin-left: auto;
    pointer: cursor;
`;